{% extends "base.html.twig" %}
{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('assets/css/landing.css') }}" type="text/css" rel="stylesheet"/>
{% endblock %}

{% block content %}
    {#{% if authUrl is defined and authUrl is not empty%}#}
        {#<a class='login' href='{{ authUrl }}'>Connect Me!</a>#}
    {#{% endif %}#}

    <header class="landing">
        <div class="col-sm-10 col-sm-offset-1 text-center">
            <h1>MUSIC SHARES YOU</h1>
            <hr>
            <h2>Melomaniacs</h2>
            <br />
            <h3 class="" data-wow-delay="0.5s">Yes, now you can do it! </h3>
            <br />
            <div class="row">
                <div class="col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
                    {% if authUrl is defined and authUrl is not empty%}
                        <a class="btn  btn-social btn-google" href="{{ authUrl }}">
                            <span class="fa fa-google"></span>
                            &nbsp;
                            Google Login
                        </a>
                    {% endif %}
                </div>
            </div>
        </div>
    </header>
{% endblock %}
