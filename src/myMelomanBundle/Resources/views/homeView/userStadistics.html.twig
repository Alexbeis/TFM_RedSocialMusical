<div class="row">
    <div class="my-following-data col-xs-6 col-sm-3 text-center">
        <a href="{{ path('show_following_users', {'id': userId}) }}">
            <h3 class="label-stat">
                Following
            </h3>
            <span class="number-stat">
             {{ userId|userStats('following') }}
            </span>
        </a>
    </div>
    <div class="my-following-data col-xs-6 col-sm-3 text-center">
        <a href="{{ path('show_follower_users', {'id': userId}) }}">
            <h3 class="label-stat">
                Followers
            </h3>
            <span class="number-stat">
            {{ userId|userStats('follower') }}
            </span>
        </a>
    </div>
    <div class="my-following-data col-xs-6 col-sm-3 text-center">
        <a href="{{ path('show_user_publications', {'id': userId}) }}">
            <h3 class="label-stat">
                Publications
            </h3>
            <span class="number-stat">
            {{ userId|getOwnPublications() }}
            </span>
        </a>
    </div>
    <div class="my-following-data col-xs-6 col-sm-3 text-center">
        <a href="{{ path('show_user_likes', {'id': userId}) }}">
            <h3 class="label-stat">
                Likes
            </h3>
            <span class="number-stat">
                {{ userId|userStats('likes') }}
            </span>
        </a>
    </div>
</div>
